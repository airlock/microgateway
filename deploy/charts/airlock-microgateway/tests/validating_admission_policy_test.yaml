suite: helm.TestValidatingAdmissionPolicy
templates:
  - validating-admission-policy.yaml
tests:
  - it: should not render admission policy
    capabilities:
      majorVersion: 1
      minorVersion: 29
    asserts:
      - hasDocuments:
          count: 0

  - it: should render admission policy, set name and object selector
    release:
      name: myoperator
      namespace: mytestnamespace
    set:
      controllerName: "microgateway.airlock.com/hans"
    capabilities:
      majorVersion: 1
      minorVersion: 30
    asserts:
      - hasDocuments:
          count: 2
      - isKind:
          of: ValidatingAdmissionPolicy
      - equal:
          path: metadata.name
          value: myoperator-microgateway-immutable-labels-mytestnamespace
        documentIndex: 0
      - equal:
          path: spec.matchConstraints.objectSelector.matchLabels
          value:
            microgateway.airlock.com/controllerName: hans
        documentIndex: 0
      - equal:
          path: metadata.name
          value: myoperator-microgateway-envoycluster-name-mytestnamespace
        documentIndex: 1